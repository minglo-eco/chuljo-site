# mulddaegak-site

## CHECKPOINT.md — 매 커밋마다 갱신한다

저장소 루트의 `CHECKPOINT.md` 는 이 프로젝트의 목표·작업순서·할일이다.
대시보드(https://checkpoint-kappa-tawny.vercel.app)가 이 파일을 읽어 사용자에게 보여준다.

**커밋할 때마다, 사용자가 시키지 않아도 스스로** 다음을 한다:

- 이번 작업으로 끝난 항목은 `- [ ]` 를 `- [x]` 로 바꾼다
- 이번 작업에서 새로 생긴 할일은 해당 `##` 단계 아래에 `- [ ]` 로 추가한다
- 목표나 작업 단계가 실제로 달라졌으면 `#` / `##` 줄도 고친다
- 바꿀 게 없으면 그냥 두고, 굳이 억지로 고치지 않는다
- 언제 끝나는지 애매한 할일에는 `(완료: 확인할 수 있는 결과)` 를 붙인다
- 남의 회신·외부 조건에 막힌 할일에는 `(대기: 무엇을 기다리는지)` 를 붙인다.
  대기가 풀리면 그 표시를 지운다. 대시보드는 대기 중인 항목을 "다음 할 일" 로 뽑지 않는다
- 아티팩트를 발행하면 `## 자료` 아래에 `- [제목](주소)` 한 줄을 추가한다

`CHECKPOINT.md` 갱신은 **별도 커밋으로 만들지 않는다.** 작업 커밋에 함께 담는다.

형식은 이 세 가지뿐이다. 다른 마크다운 문법은 대시보드가 무시한다.

```markdown
# 목표 한 줄
## 1. 작업 단계
- [ ] 할일
```

위에서부터 첫 번째 미완료 항목이 대시보드에 "다음 할 일"로 뜬다.
우선순위가 바뀌면 항목 순서를 옮긴다.
